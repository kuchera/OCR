(* Module Graph *)

class graph param = (* graph : liste du nombre de neurones par couche en commencant pas la couche la plus basse *)
object (this)
	val net =
		let l = Array.length graph in
		Array.init l (fun i ->
			
end
